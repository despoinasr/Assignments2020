language: java

branches:
    only:
    - development
    - master

sudo: required

before_install:
    - sudo apt update
 
install:
    - sudo apt install maven -y

script:
    - mvn clean package
    
after_success:
    - echo "Process terminated successfully"
    


